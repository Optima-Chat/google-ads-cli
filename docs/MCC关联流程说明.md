# Google Ads MCC 关联流程完整说明

## 🎯 流程概览

创建客户 Google Ads 账号后，需要建立 **MCC 关联**，才能让 Agency 管理客户的广告账号。

---

## 📊 完整流程图

```
┌─────────────────────────────────────────────────────────────┐
│  步骤 1: 客户创建 Google Ads 账号                            │
│                                                               │
│  客户访问 https://ads.google.com/                             │
│  → 用 Gmail 登录                                              │
│  → 切换到"专家模式"                                           │
│  → 创建账号（不创建广告系列）                                 │
│  → 获得 Customer ID (123-456-7890)                           │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 2: CLI 保存配置并发送 MCC 关联邀请                      │
│                                                               │
│  google-ads account create \                                 │
│    --email client@gmail.com \                                │
│    --name "Client Company" \                                 │
│    --customer-id 1234567890                                  │
│                                                               │
│  CLI 自动：                                                   │
│  1. ✅ 保存 Customer ID 到配置                               │
│  2. ✅ 调用 API 创建 CustomerClientLink (PENDING)            │
│  3. ✅ Google 自动发送邀请邮件到 client@gmail.com            │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 3: 客户接受邀请                                         │
│                                                               │
│  客户检查邮箱 client@gmail.com                                │
│  → 找到 "Google Ads 邀请" 邮件                                │
│  → 点击邮件中的 "接受邀请" 链接                               │
│  → 在 Google Ads UI 中确认接受                               │
│  → CustomerManagerLink 状态变为 ACTIVE                        │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  步骤 4: 设置账单并开始投放广告                               │
│                                                               │
│  客户设置付款方式（信用卡/PayPal）                            │
│  → Agency 可以通过 MCC 管理客户广告                           │
│  → 开始创建广告系列                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 技术实现

### API 调用流程

#### 步骤 1: MCC 发送邀请 (PENDING)

```typescript
// src/lib/google-ads-client.ts: sendLinkInvitation()

// 使用 MCC 账号认证
const manager = await getCustomer(MCC_CUSTOMER_ID);

// 创建 CustomerClientLink
const operations = [
  {
    entity: 'customer_client_link',
    operation: 'create',
    resource: {
      client_customer: `customers/${clientCustomerId}`,
      status: 'PENDING', // 发送邀请
    },
  },
];

await manager.mutateResources(operations);

// 查询 manager_link_id
const links = await manager.query(`
  SELECT customer_client_link.manager_link_id
  FROM customer_client_link
  WHERE customer_client_link.client_customer = 'customers/${clientCustomerId}'
`);

const managerLinkId = links[0].customer_client_link.manager_link_id;
```

#### 步骤 2: Google 自动发送邮件

创建 `CustomerClientLink` 后，Google 会自动：
- ✅ 发送邀请邮件到客户的 Gmail
- ✅ 邮件包含"接受邀请"链接
- ✅ 链接到 Google Ads UI 的授权页面

#### 步骤 3: 客户接受邀请 (ACTIVE)

客户点击邮件链接后，在 Google Ads UI 中接受：
- Google 自动将 `CustomerManagerLink` 状态设置为 `ACTIVE`
- MCC 账号获得客户账号的管理权限

---

## 📋 CLI 命令示例

### 非交互式模式（AI 使用）

```bash
google-ads account create \
  --email startup@gmail.com \
  --name "Startup Company" \
  --customer-id 1234567890 \
  --json
```

**输出**:
```
✅ 配置已保存！
📤 正在发送 MCC 关联邀请...
✅ MCC 关联邀请已发送！

{
  "success": true,
  "customer_id": "123-456-7890",
  "account_name": "Startup Company",
  "invitation_sent": true,
  "next_steps": [
    "检查邮箱中的 Google Ads 邀请邮件",
    "点击邮件中的链接接受邀请",
    "登录 Google Ads 设置账单"
  ]
}
```

### 交互式模式（人类用户）

```bash
google-ads account create --email startup@gmail.com --name "Startup Company"

# CLI 显示创建步骤
# 用户输入 Customer ID
# CLI 自动发送邀请
```

---

## 🔍 验证 MCC 关联状态

### 查询关联状态

```bash
# 查询客户账号的 MCC 关联
google-ads query -q "
  SELECT
    customer_manager_link.manager_customer,
    customer_manager_link.status
  FROM customer_manager_link
" --pretty
```

**输出示例**:
```json
[
  {
    "customer_manager_link": {
      "manager_customer": "customers/1312865623",
      "status": "ACTIVE"
    }
  }
]
```

**状态说明**:
- `PENDING`: 邀请已发送，等待客户接受
- `ACTIVE`: 客户已接受，MCC 可以管理
- `CANCELLED`: 邀请被取消
- `REFUSED`: 客户拒绝邀请

---

## ⚠️ 错误处理

### 错误 1: 账号不存在

```bash
google-ads account create \
  --email test@gmail.com \
  --name "Test" \
  --customer-id 9999999999
```

**错误**:
```
✅ 配置已保存！
📤 正在发送 MCC 关联邀请...
⚠️  发送 MCC 关联邀请失败

原因: 客户账号不存在或无法访问
您可以稍后在 Google Ads UI 中手动接受关联邀请
```

**解决方案**:
- 检查 Customer ID 是否正确
- 确认客户账号已创建成功
- 如果账号确实存在，可以稍后手动关联

---

### 错误 2: 已经关联

```bash
google-ads account create \
  --email test@gmail.com \
  --name "Test" \
  --customer-id 1234567890
```

**错误**:
```
⚠️  发送 MCC 关联邀请失败

原因: 该客户账号已经与 MCC 关联
```

**解决方案**:
- 无需重复发送邀请
- 直接开始使用 CLI 管理广告

---

### 错误 3: MCC 权限不足

```bash
google-ads account create \
  --email test@gmail.com \
  --name "Test" \
  --customer-id 1234567890
```

**错误**:
```
⚠️  发送 MCC 关联邀请失败

原因: MCC 账号没有发送邀请的权限
```

**解决方案**:
- 检查 MCC 账号是否为管理员账号
- 检查 Developer Token 权限
- 联系 Google Ads 支持

---

## 🔄 手动关联（备选方案）

如果 API 发送邀请失败，可以手动关联：

### 方法 1: MCC 发送邀请（推荐）

1. Agency 登录 Google Ads MCC 账号
2. 进入 **账号** → **子账号**
3. 点击 **+** → **关联现有账号**
4. 输入客户的 **Customer ID**
5. 选择访问级别：**管理员访问**
6. 点击 **发送邀请**

### 方法 2: 客户主动请求关联

1. 客户登录自己的 Google Ads 账号
2. 进入 **设置** → **账号访问权限**
3. 点击 **+** → **邀请用户**
4. 输入 Agency 的 **MCC Customer ID**
5. 选择访问级别：**管理员**
6. 点击 **发送邀请**

---

## 📊 关联状态检查清单

完成创建账号后，检查以下项目：

- [ ] ✅ Customer ID 已保存到 CLI 配置
- [ ] ✅ MCC 关联邀请已发送（或手动发送）
- [ ] ✅ 客户收到邀请邮件
- [ ] ✅ 客户点击邮件链接并接受邀请
- [ ] ✅ 关联状态为 `ACTIVE`
- [ ] ✅ 客户设置了账单信息
- [ ] ✅ Agency 可以查看客户账号
- [ ] ✅ 可以创建广告系列

---

## 🎯 常见问题

### Q1: 为什么需要 MCC 关联？

**A**:
- 不关联：客户只能自己管理广告
- 关联后：Agency 可以通过 MCC 帮助管理
- 类似于授权 Agency 访问你的账号

### Q2: 邀请邮件在哪里？

**A**:
- 检查客户的 Gmail 收件箱
- 可能在 **垃圾邮件** 或 **促销** 文件夹
- 邮件主题：「Google Ads 邀请」

### Q3: 不接受邀请会怎样？

**A**:
- 客户仍然可以自己管理广告
- Agency 无法通过 MCC 访问
- 邀请会在 30 天后过期

### Q4: 接受邀请后可以撤销吗？

**A**:
- 可以，客户可以在 Google Ads UI 中移除 Agency 访问权限
- Agency 也可以取消关联

### Q5: 客户会失去账号控制权吗？

**A**:
- 不会！客户始终拥有账号完全控制权
- Agency 只是获得管理权限
- 客户可以随时撤销 Agency 访问

---

## 📚 相关资源

**Sources:**
- [Linking to Manager Accounts | Google Ads API](https://developers.google.com/google-ads/api/docs/account-management/linking-manager-accounts)
- [Link accounts to your manager accounts - Google Ads Help](https://support.google.com/google-ads/answer/7459601?hl=en)
- [Manager Accounts (MCC) Documentation](https://support.google.com/google-ads/answer/6139186?hl=en)

---

## ✅ 总结

**完整流程**:
1. ✅ 客户创建独立 Google Ads 账号
2. ✅ CLI 保存配置并自动发送 MCC 邀请
3. ✅ 客户收到邮件并接受邀请
4. ✅ 关联建立，Agency 可以管理客户广告

**自动化程度**:
- 🤖 **步骤 2 完全自动化**（API 发送邀请）
- 👤 **步骤 1 和 3 需要客户操作**（创建账号、接受邀请）

**关键点**:
- 发送邀请失败**不影响**账号配置
- 可以手动在 UI 中关联
- 客户始终拥有账号控制权

---

Happy linking! 🚀
